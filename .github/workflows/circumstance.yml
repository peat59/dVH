name: determination

on:
  push:

jobs:
  free:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        dot: ["routine", "pardon", "domestic", "conscience", "combat"]
        brass: ["handle", "isolate", "expense", "distinct"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java --enable-native-access=ALL-UNNAMED -jar wN.jar
